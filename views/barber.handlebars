<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{available_barbers}}</p>
  <p>{{select_service}}</p>
  <p>{{price}}</p>
  <p>{{select_data}}</p>
  <p>Appointed created by {{user.name}} on {{format_date date_created}}</p>
    <button class="btn btn-primary">Reschedule this appointment!</button>
  </p>
</div>
