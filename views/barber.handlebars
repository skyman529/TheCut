<div class="text-center">
  <h2>{{barber_name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{haircut_type}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount haircut_price}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Reschedule this Appointment!</button>
  </p>
</div>